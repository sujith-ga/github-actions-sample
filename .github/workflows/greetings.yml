name: Greetings

on: [push]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "Hello, from GitHub Actions!"
      - name: Say bye
        run: echo "Goodbye, from GitHub Actions!"
